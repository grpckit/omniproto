syntax = "proto3";

package base;

option go_package = "github.com/grpckit/omniproto/gen";

// Base just does its thing
message Base {
  // ID is the ID of the User
  string id = 1;
  // name is a text field describing the account
  string name = 2;
}
